package com.cg.mini.dao;

public class QueryConstants {
	//static String loginQuery = "select from employee where username=?,password=?";
	static String addQuery = "insert into employee values (?,?,?,?,?,?,?,?,?,?,?,?,?,?)";

}
